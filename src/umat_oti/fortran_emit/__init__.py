"""Emitters that produce compilable OTI Fortran drivers.

Each submodule of :mod:`umat_oti.fortran_emit` produces a self-contained
Fortran build tree tied to one canonical :class:`DerivativeRequest` kind.
"""
